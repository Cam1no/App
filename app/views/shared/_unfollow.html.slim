= form_for(current_user.following_relationships.find_by(following_id: user.id), remote: true, url: ajax_user_follow_path(user.id), html: { method: :delete }) do |f|
  = f.submit "Unfollow", class: "btn btn-large btn-danger"
