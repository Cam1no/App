= form_for(current_user.following_relationships.build(following_id: user.id), remote: true, url: ajax_user_follows_path, html: { method: :post }) do |f|
  = f.hidden_field :following_id
  = f.submit "Follow", class: "btn btn-large btn-primary"
